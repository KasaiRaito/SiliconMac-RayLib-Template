{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build OSX",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/bin/build_osx",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build OSX",
            "MIMode": "lldb",
            "stopAtEntry": true
        }
    ]
}
